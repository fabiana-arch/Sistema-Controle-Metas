# Porta HTTP (o mesmo servidor atende API, página e WebSocket)
PORT=3000

# Segredo para assinar tokens JWT (obrigatório em produção — use string longa e aleatória)
JWT_SECRET=altere_para_um_segredo_longo_e_unico

# Caminho opcional do SQLite (padrão: ./data/app.db)
# SQLITE_PATH=./data/app.db
